# A simple CLI to-do list in Golang

Uses JSON for simple data persistence in storage.json.

## Use
- clone repo and run `go build` to generate executable
- run `./togolist`
- choose from available options to create and manage todos

